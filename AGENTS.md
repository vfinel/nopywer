# nopywer Agent Notes

- Run project commands with `uv run ...` from this directory.
- Run tests with `uv run pytest` (not system/global `pytest`).
- Run lint checks with `uv run ruff check .`.
- Avoid invoking a random system Python for this repo unless explicitly requested.
